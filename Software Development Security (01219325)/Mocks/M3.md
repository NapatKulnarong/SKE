# Mock — Intro to Security Standards

**Source:** [[3 - Intro to Security Standards]]  
**Format:** 40 multiple choice. One best answer. Answers at the end.

---

**1.** SSDLC is described as:

- A. A separate process that replaces the normal SDLC
- B. Bolting a **security activity onto every stage** of the normal SDLC
- C. Only pentesting after release
- D. ISO 27001 Annex A with no coding practices

**2.** At **Planning & Requirements**, the security activity added is:

- A. Threat modeling
- B. **Risk assessment** and **security requirements**
- C. Hardening
- D. Patching and incident response

**3.** At **Design**, the security activity added is:

- A. Code scanning
- B. **Threat modeling**
- C. Pentest
- D. Secret management only

**4.** At **Implement**, the security activities added are:

- A. Hardening and DR
- B. **Secure coding** and **code scanning**
- C. Risk assessment only
- D. Peer review of production logs only

**5.** At **Test**, the lecture lists:

- A. Threat modeling and ISMS policy
- B. **Code test, security test, pentest**
- C. MFA, JWT, Keycloak
- D. NIOSH transmission-rate scoring

**6.** **Deployment** adds **hardening**. **Maintenance & Support** adds:

- A. Threat modeling
- B. **Patching, monitoring, incident response**
- C. Only parameterized queries
- D. Writing the Constitution

**7.** The risk formula in this lecture is:

- A. Risk = Confidentiality × Integrity
- B. Risk = **Likelihood × Exposure (or Impact)**
- C. Risk = Bug × Exploit
- D. Risk = Annex A × OWASP Top 10

**8.** **Likelihood** is:

- A. How much damage happens if the attack succeeds
- B. How **likely** someone is to actually **attack** this thing
- C. SLA downtime only
- D. Whether a UI button is hidden

**9.** **Exposure** is:

- A. How popular the software stack is
- B. How much **damage** happens if the attack **succeeds**
- C. The number of SAST tools you run
- D. Whether pentest is graded

**10.** GPS box on the network, and an admission system with minimal input verification, are both scored:

- A. 6 (medium/high)
- B. **2** (low/medium combinations as in the table)
- C. 6–9 (PDPA)
- D. 9 only

**11.** LAN loop in a department and a registration data leak are both scored **6**. **PDPA violations** are scored:

- A. 2
- B. **6–9** (high / medium-high)
- C. 1
- D. Unscored because they are civil only

**12.** A **likelihood** factor in the lecture is software-stack **popularity**, because:

- A. Popular stacks are never exploited
- B. Popular = **more exploited**, even for your own code
- C. Popularity is an exposure factor (SLA)
- D. Popularity replaces ISMS

**13.** Which of these is an **exposure** factor, not a likelihood factor?

- A. Industry risk (banking vs general trade)
- B. Known incident rate
- C. **Legal liability** (civil/criminal, HIPAA, GDPR fines)
- D. Risk of opportunistic attack

**14.** Damage to **employee health/life**, including psychological, is listed under:

- A. Likelihood / industry risk
- B. **Exposure**
- C. Secure by default
- D. PortSwigger labs

**15.** The **NIOSH** example shows:

- A. Risk is always a 2-variable matrix only
- B. A more **complex model** can combine several factors (e.g. transmission + vaccination + contact + transport) when a simple 2-variable matrix is not precise enough
- C. That pentest is graded
- D. That hiding UI buttons is authorization

**16.** A system with no connections and no functionality is “perfectly secure” but:

- A. Required by ISO A.9
- B. **Useless**; every feature you add expands what needs protecting
- C. The definition of fail securely
- D. The same as SAST

**17.** Adding **in-app payments** to a simple library reservation system:

- A. Does not change obligations
- B. Brings in **new obligations** (convenience vs security)
- C. Replaces threat modeling
- D. Makes pentest ungraded automatically

**18.** OWASP threat-modeling question **“What are we working on?”** is for:

- A. Prioritizing risks and defining mitigations
- B. Understanding **scope, system, users, dependencies, trust boundaries**
- C. Reviewing outcomes after the fact only
- D. Running Nessus

**19.** **“What can go wrong?”** is for:

- A. Hardening production
- B. Identifying **threats, misuse cases, security/privacy concerns**
- C. Appointing a DPO
- D. Choosing Splunk vs ELK

**20.** **“What are we going to do about it?”** is for:

- A. Understanding users and trust boundaries
- B. **Prioritizing risks** and **defining mitigations**
- C. Only logging after release
- D. Writing generic error messages

**21.** **“Did we do a good job?”** is for:

- A. The first time you write requirements
- B. **Reviewing outcomes, tracking decisions, revisiting risks over time**
- C. Parameterizing SQL only
- D. Installing Keycloak

**22.** The lecture’s order is:

- A. Secure coding → then ISMS → then design
- B. **ISMS** (policy and risk posture) → **Secure Design** (architecture, data flows, trust boundaries) → **Secure Coding** (implement those decisions)
- C. Pentest → then threat modeling → then requirements
- D. OWASP Top 10 → then skip design

**23.** **Least privilege** means:

- A. Use multiple independent controls
- B. Grant **only** the permissions **needed**
- C. Default state should be unsafe so users notice
- D. Failure may grant admin access

**24.** **Defense in depth** in this lecture means:

- A. Hide the admin button in the UI
- B. Use **multiple independent controls**
- C. Remove all logging
- D. Build your own crypto

**25.** **Secure by default** means the **default state** should be **safe**. **Minimize attack surface** means:

- A. Add every payment feature immediately
- B. **Remove unnecessary** interfaces/features
- C. Treat all inputs as equally trusted
- D. Log session tokens for debugging

**26.** **Fail securely** means:

- A. Failure must **never accidentally grant access**
- B. Failure should fail open for availability
- C. Stack traces should be shown to users
- D. SAST replaces design

**27.** **Trust boundaries** means:

- A. Grant all permissions needed later
- B. Treat **lower-trust** inputs/systems **cautiously**
- C. Skip validation if TLS is on
- D. ISO A.18 only

**28.** A **role-based** access model at design becomes, in code:

- A. Hiding a UI button
- B. **Server-side authorization checks**
- C. Generic error messages only
- D. Falco on the host

**29.** An **untrusted input** boundary at design becomes:

- A. TLS only
- B. **Validation + parameterized queries**
- C. Token rotation only
- D. Annex A.12 logging

**30.** Sensitive-data requirements map to **TLS + hashing + secret management**. Safe failure behavior maps to:

- A. Restricted service/DB accounts
- B. **Generic errors, protected logs**
- C. Okta and Azure AD
- D. PortSwigger labs

**31.** The escalation chain is:

- A. Impact → Exploit → Vulnerability → Bug
- B. **Bug (incorrect behavior) → Vulnerability (security weakness) → Exploit (attacker uses it) → Impact (data/service loss)**
- C. SAST → Peer review → Monitoring
- D. ISMS → OWASP → ISO

**32.** Input validation must be **server-side** (type, length, range, format). Prefer **allowlists** (what is acceptable). This helps prevent:

- A. Only PDPA age limits
- B. **SQL injection, XSS, command injection**
- C. Only SLA violations
- D. Only employee psychological harm

**33.** Keep **data separate from commands** using:

- A. String-concatenated SQL
- B. **Parameterized queries** and safe APIs
- C. Client-side validation only
- D. Rolling your own crypto

**34.** Hiding a **UI button** is **not** the same as:

- A. TLS in transit
- B. Enforcing **server-side authorization**
- C. Patching dependencies
- D. NIOSH scoring

**35.** **Authentication** answers “Who are you?” **Authorization** answers “What can you do?” **Least privilege** answers:

- A. Did we do a good job?
- B. **How much access do you actually need?**
- C. What is the incident rate?
- D. Which OWASP cheat sheet maps to A.12?

**36.** **TLS** encrypts data **in transit**. Password hashing & salting means a leak does not expose actual passwords. **Secret management** means:

- A. Put API keys in source
- B. Keep **API keys and credentials out of code**; store them somewhere secure
- C. Show stack traces to users
- D. Log DB details

**37.** Use **trusted crypto libraries**; don’t build your own. Also watch for **outdated hashing algorithms**, not just missing ones. Errors must be **generic** (no stack traces/secrets). Logs must **never** include:

- A. Security events
- B. **Session tokens, passwords, API keys, or DB details**
- C. Timeouts and logout
- D. Peer-review comments

**38.** **SAST** is automated **static** code analysis. **Security testing** checks **behavior in a running system**. The lecture’s warning:

- A. Tools replace security reasoning
- B. **Tools assist developers — they don’t replace security reasoning**
- C. Monitoring is unused after release
- D. Peer review only checks ISO Annex A

**39.** A **pentest** is legally **simulating an attack** to find weaknesses **before** real attackers. In this course it is **optional/ungraded**. PortSwigger Web Security Academy is tied to **Burp Suite**. Risk is also inherited along:

- A. Only Annex A.9
- B. **Application code → libraries/frameworks → runtime/server → configuration**
- C. Only NIOSH factors
- D. Only GitHub publication

**40.** ISO **A.9 Access Control** maps to OWASP ASVS (AuthN/AuthZ) and tools like Okta, Azure AD, Keycloak. **A.14 Secure Development** maps to SAMM, ASVS, Threat Modeling and tools like:

- A. Splunk, ELK, Wazuh, Falco
- B. **SonarQube, Fortify, CodeQL, OWASP Threat Dragon**
- C. Only DLP and Vault
- D. Only Hack The Box machines

---

## Answer key

| # | Answer | Point |
|---|--------|--------|
| 1 | B | SSDLC bolts onto SDLC |
| 2 | B | Planning: risk + requirements |
| 3 | B | Design: threat modeling |
| 4 | B | Implement: coding + scanning |
| 5 | B | Test: code/security/pentest |
| 6 | B | Maintenance: patch, monitor, IR |
| 7 | B | Risk = L × E |
| 8 | B | Likelihood |
| 9 | B | Exposure |
| 10 | B | Score 2 scenarios |
| 11 | B | PDPA 6–9 |
| 12 | B | Stack popularity |
| 13 | C | Exposure: legal liability |
| 14 | B | Exposure: health/life |
| 15 | B | NIOSH multi-factor |
| 16 | B | Perfect security is useless |
| 17 | B | Payments expand obligations |
| 18 | B | TM: what are we working on |
| 19 | B | TM: what can go wrong |
| 20 | B | TM: what will we do |
| 21 | B | TM: did we do a good job |
| 22 | B | ISMS → design → coding |
| 23 | B | Least privilege |
| 24 | B | Defense in depth |
| 25 | B | Minimize attack surface |
| 26 | A | Fail securely |
| 27 | B | Trust boundaries |
| 28 | B | RBAC → server authZ |
| 29 | B | Untrusted input → validation |
| 30 | B | Fail securely → generic errors |
| 31 | B | Bug → vuln → exploit → impact |
| 32 | B | Injection prevented |
| 33 | B | Parameterized queries |
| 34 | B | UI ≠ server authZ |
| 35 | B | Least privilege question |
| 36 | B | Secret management |
| 37 | B | Never log secrets |
| 38 | B | Tools ≠ reasoning |
| 39 | B | Inherited risk chain / pentest |
| 40 | B | ISO A.14 tools |
