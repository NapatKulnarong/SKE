# Mock — Cryptography & Public Key Infrastructure

**Source:** [[4 - Cryptography & Public Key Infrastructure]]  
**Format:** 40 multiple choice. One best answer. Answers at the end.

---

**1.** Cryptography is a mathematical method to transform data so unauthorized people cannot:

- A. Route packets (Availability)
- B. **Read or modify** it
- C. Issue ISO 27001 certificates
- D. Run DAST

**2.** **Eavesdropping** is:

- A. Altering a transaction amount in transit
- B. **Secretly reading** data in transit or storage without permission (e.g. sniffing)
- C. Forging a signed message
- D. Revoking a CA certificate

**3.** **Data tampering** is unauthorized **alteration** in transit or storage. **Data falsification** is:

- A. Sniffing HTTPS
- B. Creating or **presenting data as genuine when it isn’t** (e.g. forging a document)
- C. Mixing paint colors in Diffie–Hellman
- D. Using ROT13 twice

**4.** On the CIA triad, cryptography contributes to:

- A. Availability only
- B. **Confidentiality and Integrity** (not Availability)
- C. All three equally
- D. Non-repudiation only, never integrity

**5.** Places you already see cryptography in the lecture include HTTPS/TLS, disk encryption (BitLocker, VeraCrypt), anti-cheat, and historically:

- A. PCI DSS 12 areas
- B. The **Enigma** machine
- C. WebGoat
- D. NIOSH

**6.** **Basic substitution** swaps each letter via a lookup table. It is:

- A. Polyalphabetic
- B. **Monoalphabetic**
- C. A stream cipher (RC4)
- D. ECC point addition

**7.** **Caesar** shifts every letter by a **fixed number**. **ROT13** is Caesar with key 13 and is:

- A. Broken because of 56-bit keys
- B. **Self-inverse**: applying it twice returns the original
- C. The same as Vigenère
- D. AES MixColumns

**8.** **Vigenère** shifts each letter by a **repeating keyword**. It is **polyalphabetic** (each letter can shift differently), so compared with Caesar it is:

- A. Easier to break
- B. **Much harder to break**
- C. A Feistel network
- D. Used as the government AES standard

**9.** Classical Caesar/Vigenère/substitution are **alphabetic**. Modern cryptography instead works on:

- A. Letters only
- B. **Bits and bytes**
- C. Paint colors only
- D. ASVS Level 1

**10.** **Symmetric** cryptography uses **one shared key** for both E and D. A lecture application is:

- A. Encrypting to Bob with Bob’s public key
- B. **WPA**: device and router share the same key
- C. A Root CA self-signing
- D. RSA \(m^e \bmod n\)

**11.** **Block** ciphers (DES, AES) operate on **fixed-size blocks**. **Stream** ciphers (RC4) operate on a:

- A. Feistel 50:50 split only
- B. **Continuous stream** of bits/bytes
- C. Certificate chain
- D. 2048-bit modulus only

**12.** AES is used in HTTPS, IPSec, disk encryption (gov’t standard). RC4 is associated with:

- A. SubBytes → ShiftRows
- B. **Legacy TLS/VPN**
- C. 4096-bit RSA
- D. Root CA pre-trust

**13.** Block vs stream **trade-off** in the lecture: block ciphers offer **higher security**; stream ciphers are:

- A. Slower
- B. **Faster — good for real-time streams**
- C. Quantum-proof
- D. Self-inverse like ROT13

**14.** **DES** is based on a **Feistel Network**: data split **50:50**, **16 rounds** of \(F\). Same circuit for E/D by reversing key order. It is **now considered broken** because:

- A. It uses 256-bit keys
- B. The **56-bit** key is **brute-forceable**; replaced by **AES**
- C. NIST still requires it after 2023
- D. It is a stream cipher KSA/PRGA

**15.** **AES** uses larger keys (**128/192/256-bit**) and processes the **whole block** each round (not half-blocks). Each round applies:

- A. KSA then PRGA
- B. **SubBytes → ShiftRows → MixColumns → AddRoundKey**
- C. \(m^e \bmod n\)
- D. Point addition three times

**16.** **RC4**: **KSA** builds a scrambled **256-byte** state; **PRGA** streams keystream bytes to **XOR** with data. It works on a sequence of bits/bytes rather than:

- A. A Diffie–Hellman color mix
- B. **Pre-cut blocks**
- C. Certificate revocation
- D. Euler’s totient

**17.** Symmetric **pros**: simple, fast, “good enough.” **Cons**: if the key leaks, **everything** encrypted with it is exposed, and the hard part is:

- A. MixColumns
- B. **Exchanging the key safely**
- C. Factoring \(n\)
- D. Finding a Root CA

**18.** **Diffie–Hellman (1976)** lets each side combine their **private** key with the other’s **public** key to get the **same shared secret** **without transmitting that secret**. The analogy is:

- A. ROT13 twice
- B. **Mixing paint colors** (mixed colors can be public)
- C. A CA issuing a name+key certificate
- D. XOR with an RC4 keystream only

**19.** NIST **disallowed 3DES from 2023** in favor of:

- A. RC4
- B. **AES**
- C. Caesar-3
- D. Basic substitution

**20.** **Asymmetric** crypto: two **mathematically linked** keys; it must be **infeasible to derive the private key from the public** one. Encrypting to Bob uses:

- A. Alice’s private key
- B. **Bob’s public key**; only **Bob’s private key** decrypts
- C. A single WPA PSK
- D. DES reverse key order

**21.** RSA’s hard problem is **factoring**: multiplying huge primes is fast; recovering \(p\) and \(q\) from \(n = p \times q\) is not. Public key is \((n, e)\); private is \((n, d)\). Anyone who **factors** \(n\) could:

- A. Only verify a signature
- B. **Reconstruct the private key**
- C. Become a Root CA
- D. Break AES MixColumns

**22.** RSA encrypt: \(\mathrm{ciphertext} = m^{e} \bmod n\). Decrypt: \(m = \mathrm{ciphertext}^{d} \bmod n\). Security needs \(n\) large enough, **2048-bit and up** today. Practical sizes listed:

- A. 56-bit only
- B. **2048, 3072, or 4096-bit**
- C. 256-bit only (that is ECC)
- D. 13 (ROT13)

**23.** ECC curve \(y^2 = x^3 + ax + b\): a line hits **exactly three points** (“point addition”). The curve is **discretized** (integer coords, modulo a huge prime). The private key is **how many times** you added the base point — the **elliptic curve discrete logarithm problem**.

- A. This is the same as RSA factoring
- B. **Forward is easy; reversing the count is extremely hard**
- C. This is Feistel 16 rounds
- D. This is KSA/PRGA

**24.** A **256-bit ECC** key is roughly as strong as a:

- A. 56-bit DES key
- B. **3072-bit RSA** key
- C. ROT13 key
- D. 128-bit AES key only

**25.** ECC is **lighter** (smaller keys, better for mobile/IoT). RSA is **heavier**. On **quantum**, Shor’s algorithm is specifically good at **factoring**, so:

- A. AES is a stream cipher
- B. **RSA is considered vulnerable** once large quantum computers exist; ECC is still generally considered better, though not immune forever
- C. Vigenère is quantum-safe
- D. PKI revocation stops Shor

**26.** Asymmetric **applications**: key exchange & authentication (then switch to **faster symmetric**), messaging/email, digital signatures. **Cons**:

- A. Too simple to implement
- B. **Computationally expensive**; more **complex in hardware**
- C. Cannot send to a specific person
- D. No public key

**27.** **Signing ≠ encryption.** A signature’s goal is **authenticity + integrity**, **not** confidentiality. Alice signs with her **private** key; Bob verifies with Alice’s:

- A. Symmetric WPA key
- B. **Public** key
- C. CA private key
- D. RC4 KSA

**28.** Digital signatures are used for software/commit signing and are legally recognized in examples such as Japan’s Electronic Signature Act and the US **ESIGN** Act. The **critical caveat**:

- A. Signatures encrypt the message
- B. A signature only proves **whoever holds this private key signed this**, **not** that the key belongs to the claimed person — **PKI** solves that
- C. Anyone with the public key can forge the signature
- D. Root CAs cannot be revoked

**29.** PKI’s problem is: with millions of public keys, how do you know a key **really belongs** to who it claims? The solution is **CAs** that issue **digital certificates** binding:

- A. A password to a disk
- B. A **public key** to a **verified identity**
- C. DES to AES
- D. ROT13 to Vigenère

**30.** Requester sends **ID + public key + proof of signing** to the CA, which issues a certificate (**name + public key**, signed by the CA). **Root CA** is:

- A. Always an in-house subordinate
- B. **Self-signed**, **pre-trusted** by OS/browser
- C. Untrusted until DAST
- D. The same as RC4 PRGA

**31.** **Subordinate CA** is trusted because a **Root CA vouches** for them. Chain: Root → Subordinate → individual certificates. **Revocation** if a private key is compromised means:

- A. The key becomes AES-256
- B. The CA **revokes** the certificate; anything signed with it **after that date** is no longer considered trustworthy
- C. Diffie–Hellman paint is reversed
- D. Factoring \(n\) becomes easy

**32.** Before trusting a Root CA, verify it is **Valid · Authentic · Trustworthy · Purposeful**. PKI applications listed:

- A. Only Caesar-3
- B. **HTTPS (SSL/TLS), SSH, email/chat encryption**
- C. Only unit testing
- D. Only Feistel rounds

**33.** **Blockchain-based PKI** would replace the central CA hierarchy with a ledger. Potential gains listed:

- A. Larger RSA keys
- B. **Zero-trust operation, faster execution, lower cost** than traditional CA fees
- C. Restoring DES
- D. Making ROT13 polyalphabetic

**34.** Will quantum break encryption? **Probably eventually** (quantum algorithms solve some hard math, e.g. factoring for RSA, far faster). It is **not an immediate threat** because quantum computers are:

- A. Cheap and everywhere
- B. **Hard to build/maintain, error-prone (noisy qubits)**; only a few major players have working ones, with bigger priorities than breaking everyday encryption
- C. Already breaking all AES-256
- D. Required for Vigenère

**35.** Post-quantum cryptography research is **active**. Takeaway: quantum is a **long-term** consideration, **not a today problem**. Symmetric crypto is often used **after** asymmetric setup because it is:

- A. Slower
- B. **Faster**
- C. Impossible to key-exchange
- D. Only alphabetic

**36.** Managing **your own** files with BitLocker uses **symmetric** crypto because:

- A. You need Bob’s public key
- B. You are **both sender and receiver** with the **same** key
- C. You need a CA for every file
- D. DES 16 rounds are required by HTTPS

**37.** Which is **not** a stated cryptography goal in §1?

- A. Prevent eavesdropping
- B. Prevent tampering
- C. Prevent falsification
- D. **Guarantee Availability** (CIA’s A)

**38.** “Easy to encrypt, easy to decrypt **with the right key**, very hard to crack” is the lecture’s shared principle for:

- A. Only Caesar
- B. **Both RSA and ECC**
- C. Only RC4
- D. Only PKI revocation

**39.** If you have \(n\), \(p\), and \(q\), deriving RSA’s \(d\) is **easy** — so you must keep:

- A. \(e\) secret only
- B. **\(p\) and \(q\) secret**
- C. The public key secret
- D. MixColumns secret

**40.** Digital signatures prove authenticity **only** if you can trust **whose** key it is. That trust is what **PKI** provides via:

- A. Feistel reverse key order
- B. **Certificate Authorities**
- C. ROT13
- D. Stream-cipher speed

---

## Answer key

| # | Answer | Point |
|---|--------|--------|
| 1 | B | What cryptography is |
| 2 | B | Eavesdropping |
| 3 | B | Falsification |
| 4 | B | CIA: C and I |
| 5 | B | Enigma / where seen |
| 6 | B | Monoalphabetic substitution |
| 7 | B | ROT13 self-inverse |
| 8 | B | Vigenère polyalphabetic |
| 9 | B | Bits and bytes |
| 10 | B | Symmetric / WPA |
| 11 | B | Stream vs block |
| 12 | B | RC4 legacy TLS |
| 13 | B | Speed vs security |
| 14 | B | DES broken 56-bit |
| 15 | B | AES round steps |
| 16 | B | RC4 KSA/PRGA |
| 17 | B | Key exchange problem |
| 18 | B | Diffie–Hellman paint |
| 19 | B | 3DES disallowed 2023 |
| 20 | B | Encrypt to Bob’s public |
| 21 | B | Factor n → private key |
| 22 | B | RSA key sizes |
| 23 | B | ECDLP |
| 24 | B | 256 ECC ≈ 3072 RSA |
| 25 | B | Shor / RSA vs ECC |
| 26 | B | Asymmetric cons |
| 27 | B | Sign vs encrypt |
| 28 | B | Signature caveat |
| 29 | B | Certificate binds identity |
| 30 | B | Root CA |
| 31 | B | Revocation |
| 32 | B | PKI applications |
| 33 | B | Blockchain PKI |
| 34 | B | Quantum not immediate |
| 35 | B | Asymmetric sets up symmetric |
| 36 | B | Own files = symmetric |
| 37 | D | Not Availability |
| 38 | B | RSA and ECC principle |
| 39 | B | Keep p, q secret |
| 40 | B | PKI / CAs |
